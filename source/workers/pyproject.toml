[project]
name = "strategy-increment-worker"
version = "0.1.0"
description = "Cloudflare runtime for simulation-only strategy increment experiments"
requires-python = ">=3.13,<3.15"
dependencies = ["fastapi==0.141.1"]

[dependency-groups]
dev = ["workers-py==1.17.4", "workers-runtime-sdk==1.9.0"]

[tool.uv]
package = false
required-version = ">=0.12.3"
